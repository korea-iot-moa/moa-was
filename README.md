🎯 프로젝트 개요
목표
대학교 졸업 후 새로운 사람들과 교류가 어려운 현실을 개선하고자, 공통 취미 기반 모임 플랫폼을 개발하였습니다.
이를 통해 언제든지 취미를 공유하고 워라밸을 유지할 수 있는 환경을 제공합니다.

주요 기능

관심사 기반 모임 추천

실시간 투표 및 리뷰 기능

다양한 사용자 경험 공유

💼 나의 역할
Back-end 개발 담당

관리자 페이지 전반 기능 구현

성별/유입률 차트 로직 개발

신고 및 블랙리스트 처리 흐름 구현

🛠 관리자 기능 요약
기능	설명
모임 수정	생성된 모임 정보를 불러와 관리자 수정 가능
투표 관리	관리자 전용 투표 등록/삭제, 결과 차트 제공
유저 리스트	유저 등급 조정 및 강제 방출 기능 제공
블랙리스트	등록/해제 가능
신고 처리	신고된 유저 방출 → 블랙리스트 등록 자동 처리
유입률 차트	성별, 분기별 유입률 차트 제공
가입 승인	유저 가입 요청에 대한 승인 기능
모임 생성	모든 입력값 유효성 검사 필수 (nullable = false)

🔍 핵심 기능 회고
📊 성별 차트 기능
findGroupByUserGenderWithCount(groupId)를 통해 성별별 인원 수 및 비율을 조회

Java Stream으로 전체 인원 기준 백분율 계산

java
복사
편집
int total = results.stream().mapToInt(r -> ((Long) r[1]).intValue()).sum();
.map(r -> new UserGenderRatioResponseDto(r[0].toString(), (Long) r[1], ((Long) r[1]) / total * 100.0))
배운 점

연산이 복잡한 경우, DB에서 처리하는 것이 성능상 유리

📈 분기별 유입률
getQuarterlyData(groupId)를 통해 가입 분기별 유입률 분석

유입 시기, 유저 수, 백분율 계산 후 차트 시각화

배운 점

복잡한 통계 데이터는 서비스보다는 DB 처리로 분산

🚫 신고 처리 로직
신고 상태 “처리 중” → “방출”로 변경

신고 내역 삭제 → 유저 삭제 → 블랙리스트 등록

java
복사
편집
if (reportResult == "방출") {
    findByUserId(userId);
    insertBlacklist(groupId, userId);
    deleteReport(groupId, userId);
    deleteUserList(groupId, userId);
}
배운 점

JPA의 연관 관계와 트랜잭션 흐름을 익히는 좋은 기회

🤝 팀 프로젝트 회고
초기 규칙 설정이 전체 프로젝트의 방향과 효율성에 결정적이었음

명확한 의사소통 방식과 역할 분담을 통해
→ 문제 상황에서도 빠른 피드백 가능
→ 일정 내 완성도 높은 결과물 도출 가능

“규칙과 소통의 체계화는 협업의 질을 좌우한다”는 중요한 교훈을 얻은 프로젝트였습니다.
